pipeline {
  agent any
  stages {
    stage('Initialize') {
      steps {
        tool 'Ant1.10.1'
      }
    }
    stage('Build') {
      steps {
        echo 'Starting Build'
        echo 'Build Complete'
      }
    }
  }
}