<?xml version="1.0" encoding="UTF-8"?>
<project name="POST TC DELIVERY PACKAGE BUILDER" default="package.delivery.post.build" >
	<property file="${basedir}/postbuild.properties" />

	<!-- Main -->
	<target name="package.delivery.post.build">
		<antcall target="package.delivery.post.build.start" />
		<antcall target="package.delivery.post.build.end" />
	</target>

	<!-- Start -->
	<target name="package.delivery.post.build.start">
		<echo>Post Build Startup</echo>
	</target>

	<!-- End -->
	<target name="package.delivery.post.build.end">
		<echo>Post Build End</echo>
	</target>

</project>